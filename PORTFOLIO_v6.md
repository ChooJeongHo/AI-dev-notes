# AI를 활용한 Android 개발 — 6차 실험 기록

**무엇을 검증할 것인가 — 나만의 검색 도구를 만들고, AI의 판단을 AI로 검증하고, "실기기가 아니면 안 보이는 것들"을 시스템으로 강제한 단계**

📅 6차 실험: 2026년 7~8월 (076~089일차)
5차 실험: [PORTFOLIO_v5.md](./PORTFOLIO_v5.md) (061~074일차)

---

## 5차에서 6차로 — 무엇이 달라졌나

| 구분 | 5차 (061~074) | 6차 (076~089) |
|------|--------------|--------------|
| 핵심 주제 | "AI 이용"에서 "AI 설계"로 전환 | AI가 만든 것을 다시 어떻게 검증할 것인가 |
| 만든 것 | 커스텀 Subagent, Hook, 멀티 에이전트 파이프라인 | 코드베이스 검색 도구(RAG), 판정 에이전트(judge), 실제 외부 기능(KOFIC 연동) |
| 반복 발견 | 070/072/073에서 "에이전트 경계=상태 경계" | 정적 사실만 확인하면 시점을 놓친다(083/088), 재사용은 스캐폴딩까지지 도메인 지식까지는 아니다(083/085/087) |
| 마무리 | 074일차 "계층 방어" 결론 | 089일차 — 직접 만들기에서 기존 스킬 쓰기로 회귀, 발견 즉시 수정까지 완결 |

---

## 6차 실험 핵심 성과

| 지표 | 수치 |
|------|------|
| 실험 수 | 14개 (076~089) |
| 만든 검색 도구 | MovieFinder 코드베이스 RAG (Room 기반, 소스+커버리지+성능 데이터 통합) |
| 만든 검증 에이전트(judge) | 2개 (코드 리뷰용, RAG 답변용) — 재검증에서 순수 환각 1건 발견 |
| 실제 신규 기능 | KOFIC 박스오피스 연동(일별+주간) + Glance 홈 화면 위젯 |
| 실기기에서만 발견된 크래시 | 3건 (081일차 2건, 086일차 관련) — 컴파일/테스트/detekt 전부 통과했던 코드 |
| Stop Hook 3부작 완성 | 073(테스트) → 087(실기기 명령) → 088(실기기 명령+시점) |
| 마지막 발견 즉시 수정 | 089일차 callTimeout 미설정 → 그 자리에서 수정+재검증 |

---

## 핵심 실험 요약

### RAG 구축 — "모른다"고 답하는 것도 설계의 일부 (076~079일차)

Room DB를 그대로 벡터 저장소로 재사용해서, MovieFinder 코드베이스에 자연어로 질문하면 답하는 도구를 만들었다.

```
076: 기본 파이프라인 완성 → "커버리지 낮은 Repository는?" 질문에
     "이 데이터로는 답할 수 없다"고 정직하게 인정
     ↓
077: 로컬 해싱 임베딩을 실제 Voyage API로 교체
     → 의미 기반 검색의 가치를 수치로 확인 (어휘 불일치 질문에서 극적 차이)
     ↓
078: JaCoCo 커버리지 데이터를 코퍼스에 통합
     → 076의 "못 푼다"던 질문을 실제로 풀어냄
     → 교훈: "AI가 모른다"의 원인은 대개 임베딩 품질이 아니라 데이터 부재
     ↓
079: 성능 데이터까지 통합 → 056일차 "에뮬레이터는 못 믿는다" 교훈이 재현
     → RAG의 근본 한계 발견: "유사도 1위 ≠ 실제 랭킹 1위"
```

**가장 중요한 발견:** 076에서 "정직하게 모른다고 답하는" AI가 078에서 "코퍼스를 넓혀서 실제로 답하는" AI로 발전했다 — 이 전환이 RAG 설계에서 가장 실용적인 교훈이었다.

---

### LLM-as-judge — 코드 리뷰에서 RAG 답변까지, 두 도메인에 적용 (080, 085일차)

070~074일차에서 만든 Subagent가 실제로 정확한 판단을 하는지, 별도의 judge 에이전트로 재검증했다.

**080일차:** 070/074일차 리뷰를 재검증하다가 순수 환각을 발견했다 — `ratingMutex.tryLock()`이 `launch` 밖에서 호출된다고 인용했는데, 실제 코드는 정반대(안전한 패턴)였다. 세 케이스 모두에서 같은 결함(체크리스트 A5 누락)이 반복되는 걸 확인하고, 원 에이전트를 즉시 보강했다.

**085일차:** 같은 judge 패턴을 RAG 답변 검증에 적용했다. 검증자(Claude) 자신이 저지른 실수(컨텍스트 밖 지식을 답변에 섞음)까지 judge가 잡아냈고, 079일차에서 발견한 "유사도≠랭킹" 함정을 재현한 의도적 오답도 정확히 감점 상한(40점)으로 판정했다.

> 재사용성의 진짜 의미: "겉모습(채점 구조, 감점 상한 규칙)은 그대로 재사용됐지만, 무엇을 검증 근거로 삼을지는 도메인마다 새로 설계해야 했다" — 이 결론이 083(KOFIC), 085(judge), 087(Stop Hook)에서 세 번 반복됐다.

---

### 실제 기능 연동 — "실기기가 아니면 안 보이는 버그"의 세 번째 변주 (081~084, 086일차)

한국 영화진흥위원회(KOFIC) 오픈API를 실제로 연동했다. 070~080일차가 "AI 내부"만 다뤘다면, 여기서 처음으로 외부 실 데이터와 UI가 등장한다.

**081일차:** 컴파일·테스트·detekt를 전부 통과한 코드가 실기기에서 두 번 크래시했다.
- KOFIC이 인증 오류를 HTTP 200 + faultInfo JSON으로 반환 — 목업으로는 재현 불가능한 실제 API 특이사항
- Material3 전용 테마 속성을 Material2 프로젝트에서 써서 인플레이트 시점 크래시 — 정적 분석으로는 절대 못 잡는 유형

**082~083일차:** N+1 문제를 해결(캐시 우선 조회)하다가 "우연히 통과하는 테스트"를 발견했고, 주간 박스오피스로 확장하면서 재사용성을 실기기 로그로 직접 증명했다.

**086일차:** Jetpack Glance로 홈 화면 위젯을 만들면서, Domain 레이어가 XML→Compose→Glance 세 번의 UI 전환에도 단 한 줄도 안 바뀌었다는 걸 실증 — Clean Architecture의 가치를 가장 직접적으로 보여준 사례.

> 056일차(성능), 079일차(같은 교훈 재현)에 이어, 081일차는 "기능 정확성" 영역에서 실기기 검증의 필요성이 세 번째로 확인된 사례였다.

---

### Stop Hook 3부작 완성 — "실기기 검증 필수"를 시스템으로 (087~088일차)

081~086일차 내내 반복된 "실기기로 직접 확인"이 사람이 매번 기억해야 하는 수동 습관이었다. 073일차 Stop Hook 원칙("결정론적 사실 확인은 차단해도 안전")을 재사용해서 이걸 시스템화했다.

```
073: 테스트 파일 존재 여부 확인 (정적 사실)
087: adb 명령 실행 여부 확인 (세션 내 이벤트 — "실행" ≠ "확인"이라는 약점 인지)
088: 실전 검증 중 "오래된 스크린샷 파일이 존재하면 통과"라는 허점 발견
     → 083일차와 같은 구조의 함정("정적 사실만 확인하면 시점을 놓친다")이 재발견됨
     → 파일 생성 시각이 세션 시작 이후인지까지 확인하도록 보강
```

**정직하게 남긴 한계:** "파일이 세션 중 생성됐다"와 "사람이 실제로 확인했다" 사이엔 여전히 간극이 있다. 이 검증 사슬은 이론적으로 끝없이 강화할 수 있지만, 지금 단계에서 의도적으로 멈췄다 — 완벽한 해법은 없고 계층적으로 방어선을 겹친다는 067/074일차의 원칙과 같다.

---

### 다시, 기존 스킬로 — 089일차의 완결

19일간 계속 "직접 만들기"만 하다가, 089일차는 방향을 바꿔 oh-my-claudecode에 이미 내장된 `/network-layer-validator`를 그대로 사용했다.

081일차에서 실제 크래시가 났던 네트워크 레이어를 KOFIC 연동 이후 처음 재점검했고, callTimeout 미설정 1건을 발견해서 그 자리에서 즉시 수정하고 재검증까지 완료했다. 검증 도구가 coroutineScope/supervisorScope를 상황별로 다르게 쓴 설계 의도를 정확히 이해해서 오탐을 스스로 피한 것도 인상적이었다.

---

## 6차 실험의 핵심 교훈

### "AI가 모른다고 답할 때, 문제는 대개 모델 성능이 아니라 데이터의 부재다"
076~078일차에서 실증됐다. 임베딩을 아무리 좋게 바꿔도 안 풀리던 질문이, 코퍼스를 확장하자 바로 풀렸다.

### "재사용은 두 층위로 나눠서 평가해야 한다 — 스캐폴딩과 도메인 지식"
083(KOFIC), 085(judge), 087(Stop Hook) 세 번 모두 같은 결론에 도달했다. 겉모습(구조, 포맷, 규칙)은 재사용되지만, "무엇을 판정 근거로 삼을지"는 매번 새로 조사해야 한다.

### "정적 사실만 확인하면 시점을 놓친다"
083일차(테스트 날짜 우연 일치)와 088일차(오래된 파일 존재)에서 같은 구조의 함정이 형태를 바꿔 재발견됐다 — "존재 여부"와 "지금 일어났는가"는 다른 질문이다.

### "실기기가 아니면 안 보이는 문제가 있다"는 세 번째로 확인됐다
056(성능), 079(같은 교훈 재현), 081(기능 정확성)까지, 컴파일/테스트/정적분석을 전부 통과해도 실기기에서만 드러나는 문제가 계속 나왔다.

### "AI가 검증한 AI도 다시 검증해야 한다"
080일차에서 070/074일차의 "정정"조차 근거가 부정확했음을 발견했다. AI의 그럴듯한 설명은 몇 번을 거쳐도 재검증이 필요하다.

---

*작성일: 2026-08-04*
