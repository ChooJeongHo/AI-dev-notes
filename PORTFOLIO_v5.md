# AI를 활용한 Android 개발 — 5차 실험 기록

**"AI를 이용하는 것"에서 "AI를 설계하는 것"으로 — 도구 활용의 정점에서 AI 시스템 설계자로 전환한 단계**

📅 5차 실험: 2026년 6~7월 (061~074일차)
1차 실험: [PORTFOLIO.md](./PORTFOLIO.md) (001~017일차)
2차 실험: [PORTFOLIO_v2.md](./PORTFOLIO_v2.md) (018~030일차)
3차 실험: [PORTFOLIO_v3.md](./PORTFOLIO_v3.md) (031~045일차)
4차 실험: [PORTFOLIO_v4.md](./PORTFOLIO_v4.md) (047~059일차)

---

## 4차에서 5차로 — 무엇이 달라졌나

| 구분 | 4차 (047~059) | 5차 (061~074) |
|------|--------------|--------------|
| 핵심 주제 | AI 에이전트 시스템 구축 (내장 기능 활용) | 내장 기능 활용 → **직접 AI를 설계하는 단계로 전환** |
| 전반부 (061~069) | — | 도구 활용의 정점: Exa/Context7 심화, 성능·보안·i18n 자동화, Compose 마이그레이션 심화 |
| 후반부 (070~074) | — | **패러다임 전환**: Subagent 직접 설계 → Hook 연동 → 멀티 에이전트 오케스트레이션 → 완결된 개선 사이클 |
| 새로 배운 것 | 모드 비교, 병렬 에이전트, 클라우드 스케줄 | **권한 구조로 역할을 강제하는 법**, 결정론적 차단 vs 확률적 정보 제공, 에이전트 경계의 실체 |

---

## 5차 실험 핵심 성과

| 지표 | 수치 |
|------|------|
| 실험 수 | 14개 (061~074) |
| 방향 전환 지점 | 070일차 — "AI 이용"에서 "AI 설계"로 |
| 직접 설계한 Subagent | 3개 (kotlin-architecture-reviewer, feature-planner, feature-implementer) |
| 완성한 Hook 타입 | 3종 (PreToolUse, PostToolUse, Stop) 전부 실전 활용 |
| 라이브러리 자동 진단 | 34개 항목 병렬 조사, 실제 위험 항목 4개만 정확히 추출 |
| 에뮬레이터 자동화 | Baseline Profile 생성 스크립트로 4분 3초 완결 |
| 접근성 감사 | MAJOR 4건 + MINOR 5건, WCAG 색상 대비 자동 계산 |
| i18n 정합성 | KO/EN 301개 키 완전 일치 달성 |

---

## 핵심 실험 요약

### 🔬 라이브러리 버전을 "숫자"가 아니라 "위험도"로 진단 (069일차)

Dependabot은 "새 버전 나왔어요"까지만 알려준다. 이번 실험은 그 판단 자체를 AI에게 맡겼다.

```
34개 라이브러리를 7개 카테고리로 나눠 병렬 서브에이전트로 동시 조사
    ↓ 약 6분 (순차 조회였다면 훨씬 오래 걸렸을 것)
30개는 이미 최신 → 실제로 손댈 건 4개뿐
```

**가장 중요한 발견 — 버전이 같아도 위험할 수 있다:**

| 라이브러리 | 버전 변화 | 왜 위험한가 |
|---|---|---|
| `security-crypto` | 변화 없음 | API 전체가 deprecated |
| `shimmer` | 변화 없음 | 저장소 자체가 2023년에 archived |

단순 `diff` 비교로는 절대 못 잡는 문제다. **버전 숫자가 아니라 생태계 건강도까지 봐야 한다**는 걸 실측으로 확인했다. Kotlin 2.4.0 + Detekt 2.0.0-alpha.5는 같이 가야 궁합이 맞지만 Hilt가 아직 2.4.0을 공식 지원한다는 근거가 없다는, **"낱개 최신화"가 아니라 "조합 검증"이 필요한 케이스**도 발견했다.

---

### 🔐 이중 방어 전략의 첫 등장 (067일차) → 074일차에서 재확인

067일차 네트워크 보안 강화에서 "OS 레벨 + 앱 레벨 이중 Certificate Pinning"을 적용했다. 이 원리("한 레이어가 뚫려도 다음 레이어가 막는다")는 **074일차 멀티 에이전트 파이프라인 설계에서 그대로 재등장**한다 — 우연이 아니라, 이 시기 실험 전체를 관통하는 설계 철학이었다.

---

### 🏗️ 070~074: "AI 이용"에서 "AI 설계"로 — 이번 사이클의 중심 서사

069개 실험 동안 반복해온 패턴은 **"이미 만들어진 AI 기능을 호출하는 것"**이었다. 070일차부터 처음으로 **"AI가 어떻게 판단할지 자체를 내가 설계"**하는 단계로 넘어갔다.

```
070: 단일 Subagent 직접 설계
     kotlin-architecture-reviewer — CLAUDE.md 규칙만 보는 좁은 스코프의 리뷰어
     → 실전 테스트에서 오탐 1건 발견 (컴파일 없이 판단해서 생긴 문제)
         ↓
071: 오탐을 실제로 고치고, Hook에 연결
     "@OptIn 지적 시 반드시 컴파일로 검증" 규칙 내장
     PreToolUse 훅으로 ViewModel 수정 시 자동 사전 검토 시스템 완성
     → 핵심 원칙 확립: "결정론적 판단만 차단, AI 판단은 정보 제공"
         ↓
072: 여러 Subagent를 체이닝 — 설계자 → 구현자 → 검증자
     "쪼개면 오버헤드로 느릴 것"이라는 직관과 반대로,
     원샷이 오히려 시간 18% 더 걸렸고, 같은 검증자가 원샷에서만 
     응집도 문제를 잡아냄 — 진짜 가치는 비용이 아니라 구조적 안전장치
         ↓
073: Stop Hook으로 훅 3부작(Pre/Post/Stop) 완성
     "테스트 없이 세션 종료 시도"를 감지해 차단
     → 실전에서 더 큰 발견: 서브에이전트가 만든 파일은 
       메인 세션 transcript에서 아예 안 보인다
     → "에이전트 경계 = 상태 경계"라는 070/072/073 공통 원인 규명
         ↓
074: 073의 한계를 사후 감시가 아니라 사전 설계로 해소
     구현자 에이전트 정의 자체에 "테스트 필수" 규칙을 못박음
     → 재설계 후에도 검증자가 체크 항목 하나를 실제로 빼먹는 걸 발견
     → 결론: 사전 설계도 100% 보장 아님 — 양자택일이 아니라 계층 방어
```

**5일간 하나로 이어진 사이클:** 설계 → 오탐 발견 → 수정 → 확장 → 예상 밖 실측 결과 → 새로운 한계 발견 → 근본 원인 규명 → 재설계 → **재설계도 완벽하지 않음을 재확인.** 어느 하나도 "한 번 만들고 끝"이 아니라, 매번 실전에서 검증하고 다음날 바로 개선한 완결된 성장 사이클이다.

---

### 🎨 Compose 마이그레이션 심화 — 하이브리드 전략 (068일차)

027일차(State 없는 단순 화면)에 이어, 이번엔 **ViewModel + Paging3 + RecyclerView가 얽힌 785줄 화면**을 마이그레이션했다. 전체를 한 번에 옮기지 않고 **검색 TextField + 결과 목록만 Compose로, 나머지는 XML 그대로 유지**하는 하이브리드 전략을 택했다.

핵심 기법: `ComposeView`로 XML 안에 Compose를 부분 이식, 상태 호이스팅으로 검색어를 Fragment 한 곳에서만 관리, RecyclerView가 사라지며 끊긴 FAB 스크롤 연동은 **콜백을 서로 전달하는 브릿지 패턴**으로 재연결.

---

## 5차 실험의 핵심 교훈

### "권한 구조로 역할을 강제하는 것이 프롬프트로 부탁하는 것보다 강하다"
070일차 `feature-planner`는 `tools`에서 Write/Edit를 아예 빼서 "계획만 세우는" 역할을 시스템적으로 강제했다. "하지 마세요"라는 프롬프트보다 훨씬 확실했다.

### "결정론적 판단은 차단해도 안전하지만, AI의 판단은 정보 제공으로 남겨야 한다"
071일차에서 확립한 이 원칙은 073(Stop Hook)에서도, 074(사전 설계)에서도 계속 유효했다 — 심지어 사전 설계로 문제를 미리 막으려 해도, AI 기반 검증 단계는 여전히 확률적이라는 게 실측으로 재확인됐다.

### "직관은 실측으로 검증해야 한다"
"파이프라인으로 쪼개면 오버헤드 때문에 느릴 것"이라는 예상은 072일차 실측에서 뒤집혔다. 원샷이 시간은 오히려 18% 더 걸렸다 — 계획 없이 바로 짜면 탐색/시행착오 비용이 계획 비용보다 컸다.

### "버전 숫자만으로는 라이브러리 위험도를 알 수 없다"
069일차에서 `security-crypto`와 `shimmer`는 버전이 그대로인데도 "리뷰 필요"로 분류됐다 — deprecated API, archived 저장소는 diff로는 절대 안 보인다.

### "안전장치는 이분법이 아니라 계층이다"
067일차(OS+앱 이중 피닝)와 074일차(사전 설계+결정론적 백스톱)에서 같은 결론에 도달했다: 한 레이어가 완벽해도 다른 레이어가 필요하다.

---

→ [전체 실험 기록 보기](./README.md)

---

*작성일: 2026-07-14*
