# AI를 활용한 Android 개발 — 7차 실험 기록

**같은 증상, 다른 원인 — 검증 스킬을 재실행해서 "클린"의 성격을 구분하고, 실기기 UI 흐름이 아니면 절대 안 보이는 버그들을 계속 찾아낸 단계**

📅 7차 실험: 2026년 8~9월 (091~104일차)
6차 실험: [PORTFOLIO_v6.md](./PORTFOLIO_v6.md) (076~089일차)

---

## 6차에서 7차로 — 무엇이 달라졌나

| 구분 | 6차 (076~089) | 7차 (091~104) |
|------|--------------|--------------|
| 핵심 주제 | 무엇을 검증할 것인가 (RAG, judge, 실기기) | 검증을 반복 실행해서, "클린"과 "이슈 있음"이 나오는 서로 다른 이유를 구분 |
| 새 기능 | KOFIC 박스오피스 연동 | KMRB 관람등급 연동 + Predictive Back/Splash/App Shortcuts 등 최신 UX 기능 |
| 반복 발견 | 정적 사실만 확인하면 시점을 놓친다 | 같은 증상(온보딩 재등장, 손댈 것 없음 등)도 원인은 매번 다르다 |

---

## 7차 실험 핵심 성과

| 지표 | 수치 |
|------|------|
| 실험 수 | 14개 (091~104) |
| 재실행한 검증 스킬 | 3개 (network-layer/material3/proguard, 모두 089/091/092일차 원본을 095~097 기준으로 재검증) |
| KMRB API 연동 관련 실험 | 3개 (095 최초 연동, 096 필터 캐시, 097 박스오피스 확장) |
| 새로 다룬 Android UX 기능 | 3개 (Predictive Back, Splash Screen API, App Shortcuts) |
| 실기기에서만 발견된 버그 | 4건 이상 (095 동시성 2건, 101 리소스 타입, 103 API 오용, 104 커버리지 누락) |
| SKILL.md 자체를 직접 수정한 횟수 | 1회 (098일차, 반복 오탐 방지 가드 추가) |

---

## 핵심 실험 요약

### 검증 스킬 3부작 재실행 — "클린"에도 세 가지 다른 얼굴이 있었다 (098~100일차)

089(network-layer), 091(material3), 092(proguard) 세 검증 스킬을 095~097일차 KMRB 신규 코드 기준으로 전부 재실행했다.

```
098 (material3 재실행): 오탐 2건 발견 — 4번째 반복
    → "이번에도 잘 걸러냈다"에서 멈추지 않고 SKILL.md 자체에
      "0단계 기준선 확인" 가드를 직접 추가
        ↓
099 (network-layer 재실행): 실질 이슈 0건
    → callTimeout이 공유 함수를 통해 자동 상속됨을 git 이력으로 확인
    → "좋은 코드 구조" 덕분에 클린
        ↓
100 (proguard 재실행): 신규 keep 규칙 불필요
    → 095일차의 SAXParser 선택(테스트 가능성 확보 목적)이
      리플렉션 경로 자체를 없애서, 부수적으로 R8 안전성까지 확보
    → "좋은 설계 결정" 덕분에 클린
```

세 재실행 결과가 전부 다른 이유로 나왔다는 게 이 시리즈의 핵심이다: 098은 검증 스킬의 한계(고쳐야 할 대상), 099는 코드 구조의 이득(그냥 좋은 것), 100은 설계 결정의 부수 효과(의도 안 했지만 좋은 것). 같은 "클린"이라는 결과 뒤에 이렇게 다른 원인이 있을 수 있다는 걸 세 번 나란히 확인한 게 이번 사이클에서 가장 성숙한 발견이었다.

---

### KMRB 연동 3부작 — 짐작의 위험성에서 완전한 재사용까지 (095~097일차)

**095일차:** AI가 웹 검색만으로 짐작한 엔드포인트(open.kmrb.or.kr)가 틀렸다. Wi-Fi와 핫스팟 양쪽에서 똑같이 막혀서야 "우리 문제가 아니라 서버 자체 문제"임을 알았고, 결국 사용자가 공공데이터포털 화면을 직접 열어서 진짜 스펙(apis.data.go.kr/B551008/movie_v3/movie_search_v3)을 찾아냈다. 이후 XML 파싱에 SAXParser를 선택(테스트 가능성 확보)하고, 검증자가 메인 스레드 블로킹·시청 기록 저장 타이밍 문제 2건을 잡아냈다.

**096일차:** 082일차의 "캐시 우선 조회" 패턴을 KMRB 검색 필터에 그대로 적용하려 했는데, KOFIC과 달리 KMRB는 애초에 캐시가 비어있어서 통하지 않았다. 패턴(사고방식)은 재사용하되 캐시 자체는 범용으로 새로 지었다.

**097일차:** 096일차가 지은 그 범용 캐시가, 박스오피스 섹션이라는 완전히 다른 사용처에도 조정 없이 그대로 재사용됐다. 082→096의 재사용 실패가 096→097에서 완전한 성공으로 이어진 것.

---

### 최신 Android UX 기능 — 실기기가 잡아내는 버그의 종류가 매번 달랐다 (101~104일차)

```
101 (Predictive Back): 커스텀 뒤로가기 코드가 아예 없었다
    → 실기기 스와이프로 확인해보니, 진짜 원인은 코드가 아니라
      리소스 타입(res/anim 레거시 vs res/animator seek 가능)
        ↓
102 (Splash Screen API): 이미 초기 커밋부터 공식 API로 구현되어 있었다
    → 101일차와 똑같이 "손댈 게 없다"는 결론이지만, 원인은 완전히 다름
    → (101=코드가 없었음, 102=처음부터 잘 되어 있었음)
        ↓
103 (App Shortcuts): 컴파일·유닛테스트 855개·detekt·아키텍처 리뷰 전부 통과
    → 그런데 실기기 롱프레스→탭→뒤로가기 흐름에서만 popBackStack API
      오용 버그 발견 — 레이어/코루틴 문제가 아니라 "API 의미 오해"
        ↓
104 (딥링크 백스택): 103일차가 남긴 선재 결함을 처리
    → 새 버그가 아니라 103일차 화이트리스트 함수가 movie/stats를
      빠뜨렸던 커버리지 누락이었음
```

같은 "실기기 UI 흐름 검증"이 매번 다른 층위의 문제를 잡아냈다: 리소스 타입, 이미 문제없음, API 오용, 커버리지 누락. 이 네 가지는 전부 코드 리뷰나 정적분석 체크리스트가 애초에 다루지 않는 범주였다.

---

## 7차 실험의 핵심 교훈

### "같은 결과(클린 또는 같은 증상)도 원인은 매번 확인해야 한다"
098/099/100(클린의 세 가지 다른 이유), 101/102(같은 결론, 다른 원인), 101/103/104(같은 증상 계열, 다른 원인)까지 — 이번 사이클에서 가장 많이 반복된 원칙이다.

### "반복되는 오탐/결함은 도구 자체를 고쳐서 원천 차단한다"
098일차에서 검증 스킬(SKILL.md)을 직접 수정한 게, 073/087/088/080일차의 원칙(Stop Hook 보강, judge 피드백 루프)이 기존 도구에도 그대로 적용된 사례였다.

### "좋은 구조/설계 결정은 의도하지 않은 곳에서도 이득을 낸다"
099일차(공유 함수 덕분에 설정 자동 상속)와 100일차(테스트 가능성 위해 고른 SAXParser가 R8 안전성도 확보)가 같은 패턴을 두 번 보여줬다.

### "정적분석을 전부 통과해도 실제 사용자 흐름을 끝까지 따라가야 한다"
103일차의 popBackStack 오용은 롱프레스→탭→뒤로가기라는 전체 흐름을 실기기에서 따라가야만 드러났다.

### "AI의 짐작은 검증 없이 믿으면 위험하다"
095일차에서 잘못된 엔드포인트 짐작으로 헤맸고, 결국 사람이 직접 확인해서 바로잡았다.

---

*작성일: 2026-09-08*
